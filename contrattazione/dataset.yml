root: "../out"
schema_version: 1

dataset:
  name: "contrattazione"
  source_id: "openbdap"
  years: [2020, 2021, 2022, 2023, 2024]

raw:
  output_policy: overwrite
  sources:
    - name: "contrattazione_csv"
      type: "local_file"
      args:
        path: "../_local/seed/dati/{year}/CONTRATTAZIONE_INTEGRATIVA_{year}.CSV"
      primary: true

clean:
  sql: "sql/clean.sql"
  read:
    mode: latest
    header: true
    delim: ";"
    encoding: "utf-8"
  validate:
    min_rows: 1000

mart:
  tables:
    - name: "contrattazione_comparti"
      sql: "sql/mart.sql"
  required_tables:
    - "contrattazione_comparti"
  validate:
    table_rules:
      contrattazione_comparti:
        min_rows: 5

validation:
  fail_on_error: true

support:
  - name: "enti"
    config: "../anagrafica/anag-enti/dataset.yml"
    years: [2024]
  - name: "comparti"
    config: "../anagrafica/anag-comparti/dataset.yml"
    years: [2024]

